from setuptools import setup

setup(
  name = 'rformat',
  version = '0.1.0',
  author = 'brian wickman',
  author_email = 'wickman@gmail.com',
  license = 'MIT',
  description = 'rformat is a recursive version of str.format',
  zip_safe = True,
  py_modules = ['rformat'],
)
